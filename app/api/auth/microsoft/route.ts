export async function POST() {
  return Response.json({
    ok: false,
    message:
      "Microsoft SSO is a placeholder in this preview. Configure Entra ID and environment variables to enable.",
  })
}
